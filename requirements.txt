# Shutdown Scheduler - Python Dependencies

# For development and building executable
pyinstaller>=6.0.0

# For system tray functionality
pystray>=0.19.0
Pillow>=10.0.0

# For single instance detection
psutil>=5.9.0

# Built-in modules used (no installation needed):
# - tkinter (GUI framework)
# - subprocess (system commands)
# - threading (concurrent operations)
# - datetime (date/time handling)
# - locale (system locale detection)
# - os (operating system interface)
# - time (time utilities) 